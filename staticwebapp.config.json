{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/static/*", "/images/*"]
  },
  "auth": {
    "rolesSource": "DEFAULT"
  },
  "routes": [
    {
      "route": "/",
      "allowedRoles": ["authenticated"]
    }
  ]
}
